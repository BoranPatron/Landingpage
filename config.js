// Optional: Setze hier die API-Basis-URL deiner Render API (Web Service)
// Beispiel: window.BUILDWISE_API_BASE = 'https://buildwise-api.onrender.com';
// Lässt du es leer, wird relativ auf /beta-subscribe gepostet (gleiche Domain/Reverse Proxy nötig)
window.BUILDWISE_API_BASE = '';


